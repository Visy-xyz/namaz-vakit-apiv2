name: Yearly Prayer Times Refresh

on:
  # Çdo 1 Janar ora 02:00 UTC
  schedule:
    - cron: '0 2 1 1 *'

  # Manual trigger — mund ta ekzekutosh edhe tani për herë të parë
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    # Full Europe + US etc. can run for many hours; increase if the job hits the limit.
    timeout-minutes: 360

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch prayer times (Europe list or data/ refresh)
        env:
          DIYANET_EMAIL: ${{ secrets.DIYANET_EMAIL }}
          DIYANET_PASS: ${{ secrets.DIYANET_PASS }}
        run: |
          if [ -f countries-all.json ] || [ -f countries.json ]; then
            echo "Using scripts/fetch-europe.mjs (countries-all.json or countries.json present)"
            node scripts/fetch-europe.mjs
          else
            echo "No country list JSON — using scripts/fetch-yearly.mjs (refresh every data/*/city.json with _meta.districtId)"
            node scripts/fetch-yearly.mjs
          fi

      - name: Build prayer catalog
        run: node scripts/build-prayer-catalog.mjs

      - name: Commit new data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: refresh prayer times ${{ github.run_number }}"
          file_pattern: |
            data/**/*.json
            generated/prayer-catalog.json
