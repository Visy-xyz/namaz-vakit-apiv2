node_modules/

# Vercel pulls short-lived OIDC tokens into .env.local — never commit any of these.
.env
.env.*
!.env.example
.vercel
.vercel/

*.log
fetch.log
status.json

# Built by the yearly refresh before publishing to the CDN.
cdn-dist/
